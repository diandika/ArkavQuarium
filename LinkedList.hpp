#ifndef LINKEDLIST_HPP
#define LINKEDLIST_HPP

template <class T>
class LinkedList {
private:
  /* data */

public:


  int find(T);
  bool isEmpty();
  void add(T);
  void remove(T);
  T getIndex(int);
};

#endif
